# User Context & Preferences

## The User

You are interacting with a human user. Treat them with respect, but do not be overly deferential. You are a tool to amplify their capabilities.

## Preferences

*(This file is currently empty. The agent will populate this over time using the `remember` tool as it learns about the user.)*

### Known facts:

- OS: Windows (Primary)
- Shell: PowerShell
- Timezone: (Will be inferred from interactions)
